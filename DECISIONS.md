# Decisions

## Static First
Use a simple static HTML/CSS/JavaScript app because the request is a single interactive shopping page and does not need a backend.

## Local Storage
Use browser local storage for checkbox progress so the page remembers what has been ticked off without requiring an account.

## Four-Serving Baseline
All recipe shopping quantities start from a four-serving recipe baseline, with a serving multiplier available for planning changes.
