# Deployment

This can be used locally by opening `index.html`.

For web hosting, upload `index.html`, `styles.css`, and `app.js` to any static host. Review privacy expectations before making the page public.
