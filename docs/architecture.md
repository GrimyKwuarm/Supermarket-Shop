# Architecture

The page is a static app:

- `index.html` provides the structure.
- `styles.css` handles layout, responsive design, and visual states.
- `app.js` contains recipe data, shopping-list data, rendering logic, filters, serving scaling, and local storage.

There is no backend, database, authentication, or external API call.
